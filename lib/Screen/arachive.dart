import 'package:flutter/material.dart';

class Arachive extends StatelessWidget {
  const Arachive({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
